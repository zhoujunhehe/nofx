# Railway 部署配置文件
# 文档: https://docs.railway.app/deploy/config-as-code

[build]
# 使用自定义的 Railway Dockerfile
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"

[deploy]
# 健康检查
healthcheckPath = "/api/health"
healthcheckTimeout = 100
